#include<stdio.h>
#include<math.h>
int sqr(int num)
{
 float a=sqrt(num);
 int b=a;
 if(a==b)
 return 1;
 else
 return 0;
}
int main()
{
 int a = 20, b = 100;
 int i=a;
while(i<=b)
{
 if(sqr(i))
 printf("%d\n",i);
 i++;
}
}
